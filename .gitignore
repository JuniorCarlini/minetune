# Segredos e estado local — nunca versionar
.env
data/
backups/

# Painel
panel/node_modules/
panel/dist/

# SO / editores
.DS_Store
.idea/
.vscode/

# Site do GitHub Pages (gerado por scripts/build-site.mjs)
_site/
# Mapa de seeds: código do cubiomes baixado e WebAssembly compilado (scripts/build-seedmap.sh)
.cache/
build/

# Destino dos backups salvo pelo painel (tem segredos)
config/backup.env

# Liga/desliga da senha do Portão Minetune (estado local, gravado pelo painel)
config/gate.json
# Pastas vazias que o app do Umbrel precisa ter no pacote
!umbrel/minetune-server/data/
